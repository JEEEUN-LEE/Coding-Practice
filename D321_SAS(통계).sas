/*------------------------------------------------------------*/
/* SAS - PROC TABULATE 활용 예제 */
/*------------------------------------------------------------*/

/* 목적: 요약 통계 테이블을 생성하는 절차 */
/* 특징:
   ▪ 다차원 교차표(2~3차원) 생성 가능
   ▪ 범주형 변수와 연속형 변수의 통계량 요약
   ▪ PROC REPORT보다 수치 요약 시 더 간편함
------------------------------------------------------------*/

/*------------------------------------------------------------*/
/* 기본 문법(Syntax)
PROC TABULATE data = 데이터셋 <옵션>;
    CLASS 범주형변수;
    VAR   연속형변수;
    TABLE 행차원, 열차원;
RUN;
------------------------------------------------------------*/

/* 예시 */
proc tabulate data = test2 missing;   /* MISSING 옵션: 결측값도 포함 */
    class ageatstart sex weight_status;   /* 범주형 변수 지정 */
    var weight;                           /* 연속형 변수 지정 */
    table ageatstart * weight_status,     /* 행차원: 나이별 × 체중상태별 */
          sex * (weight * mean);          /* 열차원: 성별 × 평균 체중 */
run;

/*------------------------------------------------------------*/
/* 주요 포인트
   ▪ CLASS 문 : 범주형 변수 지정 (집단 구분 기준)
   ▪ VAR 문   : 요약할 연속형 변수 지정
   ▪ TABLE 문 : 행, 열 기준으로 교차 요약 구조 정의
   ▪ MISSING 옵션 : 결측값을 분석에 포함
   ▪ weight*mean : weight 변수의 평균(mean)을 계산
------------------------------------------------------------*/
